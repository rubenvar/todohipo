extends layout

block content
  h1 Choose tip to edit:
  form.choose-tip(action="/update-form" method="POST")
    select(name="tip")
      each tip in tips
        option(value=`${tip._id}`) #{tip.name}
    input(type="submit" value="Edit")