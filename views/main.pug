extend layout

include mixins/_tipCard

block vars
  - bodyClass = 'main';
  if author
    - photoCredit = author;
  else
    - photoCredit = { user: 'NordWood Themes', link: 'https://unsplash.com/@nordwood' }
block cover
  .cover(style=`background: linear-gradient(rgba(255,255,255,0.5), rgba(255,255,255,0.9)), url('${photo}') no-repeat fixed center; background-size: cover;`)
    .wrapper
      - const num = Math.floor((tips.length - 1) / 5) * 5;
      h1 Deja ya de buscar cómo se quita el hipo!
      p
        em ¿Acaba de darte un ataque y quieres curarlo AHORA MISMO?
      h2 👇 Mira aquí más de #{num} remedios para el hipo 👇
      p Elige uno, lee las instrucciones, y pruébalo
      p Si no te funciona... Intenta con otro!
block content
  .wrapper
    .tips
      each tip, index in tips
        if index < 15
          +tipCard(tip, index)
  .cover.inter-block(style=`background: linear-gradient(rgba(255,255,255,0.9), rgba(255,255,255,0.5), rgba(255,255,255,0.9)), url('${photo}') no-repeat fixed center; background-size: cover;`)
    .wrapper
      p ¿Has probado ya alguno de los #{tips.length} remedios?
      h2 🗳️ Deja tu voto en cada consejo que intentes! 🗳️
      p Así otros lectores sabrán si funciona
  .wrapper
    .tips
      each tip, index in tips
        if index > 14
          +tipCard(tip, index)