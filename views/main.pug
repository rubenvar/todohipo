extend layout

include mixins/_tipCard

block vars
  - bodyClass = 'main';
block cover
  .cover(style=`background: linear-gradient(rgba(255,255,255,0.5), rgba(255,255,255,0.9)), url('${photo}');`)
    if author
      p Photo by 
        a(href=`${author.link}` target="_blank" rel="nofollow") #{author.user}
        |  on  
        a(href="https://unsplash.com" target="_blank" rel="nofollow") Unsplash
    .wrapper
      - const num = Math.floor(tips.length / 5) * 5;
      h1 Deja ya de buscar cómo se quita el hipo!
      p.big
        em ¿Acaba de darte un ataque y quieres curarlo AHORA MISMO?
      h4 👇 Mira aquí más de #{num} remedios para el hipo 👇
      p Elige uno, lee las instrucciones, y pruébalo
      p Si no te funciona... Intenta con otro!
block content
  .tips
    each tip, index in tips
      if index < 20
        +tipCard(tip, index)
  p Aquí va un bloque!
  .tips
    each tip, index in tips
      if index > 19 && index < 40
        +tipCard(tip, index)
  p Aquí va un bloque!
  .tips
    each tip, index in tips
      if index > 39
        +tipCard(tip, index)
  .end-disclaimer
    p todohipo.com no es una página médica y no se hace responsable de nada de lo que te pase. Si tienes problemas de salud, no deberías probar estos métodos. Si tu hipo persiste, ve al médico.