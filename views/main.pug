extend layout

include mixins/_tipCard

block vars
  - bodyClass = 'main';
  if author
    - photoCredit = author;
  else
    - photoCredit = { user: 'NordWood Themes', link: 'https://unsplash.com/@nordwood' }
block cover
  .cover(style=`background: linear-gradient(rgba(255,255,255,0.5), rgba(255,255,255,0.9)), url('${photo}') no-repeat fixed center; background-size: cover;`)
    .wrapper
      - const num = Math.floor(tips.length / 5) * 5;
      h1 Deja ya de buscar cómo se quita el hipo!
      p.big
        em ¿Acaba de darte un ataque y quieres curarlo AHORA MISMO?
      h4 👇 Mira aquí más de #{num} remedios para el hipo 👇
      p Elige uno, lee las instrucciones, y pruébalo
      p Si no te funciona... Intenta con otro!
block content
  .tips
    each tip, index in tips
      if index < 20
        +tipCard(tip, index)
  p Aquí va un bloque!
  .tips
    each tip, index in tips
      if index > 19 && index < 40
        +tipCard(tip, index)
  p Aquí va un bloque!
  .tips
    each tip, index in tips
      if index > 39
        +tipCard(tip, index)